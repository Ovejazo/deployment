apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-bdespecialday-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/db-bdespecialday-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-bdespecialday-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-bdespecialday-deployment
  labels:
    app: db-bdespecialday-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: db-bdespecialday-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: db-bdespecialday-deployment
        tier: database
    spec:
      containers:
        - name: bdespecialday
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: db-bdespecialday-data
      volumes:
        - name: db-bdespecialday-data
          persistentVolumeClaim:
            claimName: db-bdespecialday-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: db-bdespecialday-service
  labels:
    app: db-bdespecialday-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: db-bdespecialday-deployment
    tier: database
  type: LoadBalancer