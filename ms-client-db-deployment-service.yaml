apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-bdclient-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/db-bdclient-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-bdclient-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-bdclient-deployment
  labels:
    app: db-bdclient-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: db-bdclient-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: db-bdclient-deployment
        tier: database
    spec:
      containers:
        - name: bdclient
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: db-bdclient-data
      volumes:
        - name: db-bdclient-data
          persistentVolumeClaim:
            claimName: db-bdclient-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: db-bdclient-service
  labels:
    app: db-bdclient-service
    tier: database
spec:
  ports:
    - port: 5433
      targetPort: 5432
      protocol: TCP
  selector:
    app: db-bdclient-deployment
    tier: database
  type: LoadBalancer