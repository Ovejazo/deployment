apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-bdfrecuencyclents-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/db-bdfrecuencyclents-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-bdfrecuencyclents-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-bdfrecuencyclents-deployment
  labels:
    app: db-bdfrecuencyclents-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: db-bdfrecuencyclents-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: db-bdfrecuencyclents-deployment
        tier: database
    spec:
      containers:
        - name: bdfrecuencyclents
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: db-bdfrecuencyclents-data
      volumes:
        - name: db-bdfrecuencyclents-data
          persistentVolumeClaim:
            claimName: db-bdfrecuencyclents-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: db-bdfrecuencyclents-service
  labels:
    app: db-bdfrecuencyclents-service
    tier: database
spec:
  ports:
    - port: 5436
      targetPort: 5432
      protocol: TCP
  selector:
    app: db-bdfrecuencyclents-deployment
    tier: database
  type: LoadBalancer