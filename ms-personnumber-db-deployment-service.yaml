apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-bdpersonNumber-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/db-bdpersonNumber-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-bdpersonNumber-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-bdpersonNumber-deployment
  labels:
    app: db-bdpersonNumber-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: db-bdpersonNumber-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: db-bdpersonNumber-deployment
        tier: database
    spec:
      containers:
        - name: bdpersonNumber
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: db-bdpersonNumber-data
      volumes:
        - name: db-bdpersonNumber-data
          persistentVolumeClaim:
            claimName: db-bdpersonNumber-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: db-bdpersonNumber-service
  labels:
    app: db-bdpersonNumber-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: db-bdpersonNumber-deployment
    tier: database
  type: LoadBalancer